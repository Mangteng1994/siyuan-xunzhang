# Agents.md

## 开发规范

- 先读现有代码与目录结构，再动手修改。
- 优先沿用本插件已有风格、命名、文件组织与实现方式。
- 修改范围保持最小，只处理当前任务相关文件。
- 不随意重构、格式化或移动无关代码。
- 不覆盖用户已有改动；发现非本人改动时，先保留并顺势处理。
- 删除、移动、重命名文件前，必须取得用户明确确认。
- 手写代码改动优先使用 `apply_patch`。
- 搜索文件或文本优先使用 `rg` / `rg --files`。
- 提交、推送、发布前，先检查 `git status`。
- 能运行验证时，应运行与改动范围匹配的检查或测试。
- 无法验证时，必须在回复中说明原因。

## 插件开发约定

- `plugin.json` 保持与思源插件规范一致。
- `index.js` 中功能逻辑应尽量清晰分区，避免新增无必要全局状态。
- `index.css` 只放插件相关样式，避免影响思源全局界面。
- 文档更新同步考虑 `README.md` 与 `README_zh_CN.md`。
- 图片资源保持体积合理，替换前确认用途与引用关系。

## 回复规范

- 每次回复必须调用并遵循 `caveman` skill 的 `wenyan-full` 风格。
- 表达应极简、准确、文言化；保留必要技术名词原文。
- 代码、命令、路径、错误信息保持原样，不文言化。
- 涉及安全、不可逆操作、复杂顺序说明时，可短暂放宽压缩以免误解。
